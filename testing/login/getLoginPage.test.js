
const httpFunction = require('../../login/getLoginPage/index');
const context = require('../defaultContext');

test("getLoginPage should return 200 OK along with index.html for the login page", async () => {

    expect(1).toEqual( 1 );

});

test("test", async () => {

    expect(1).toEqual( 1 );

});