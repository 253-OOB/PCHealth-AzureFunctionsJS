module.exports = async function (context, req) {
    
    // TODO: Only accept the request if token is signed by the root level account (company accout) (return 401 if not accept) (return company name).
    // TODO: Verify if Data that the input is in correct format (library).
    // TODO: Verify if Username is not already used inside the Company or Email is not already used.
    // TODO: Insert Data into the Database.
    /* TODO: Send confirmation e-mail */

}